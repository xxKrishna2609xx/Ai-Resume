/**
 * MobileActionBar is now a no-op stub.
 * Mobile navigation is handled by the AppShell component's
 * built-in bottom tab bar and mobile header.
 *
 * This file is kept to avoid breaking any legacy imports.
 */
export function MobileActionBar() {
  return null;
}
